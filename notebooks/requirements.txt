# ── Training notebook (train_classifier.ipynb) ────────────────────────────
# Install with: pip install -r notebooks/requirements.txt
# Run on Google Colab (free T4 GPU) or a local CUDA machine.

# Core ML
ultralytics>=8.0          # YOLOv8 training + export
torch>=2.0                # required by ultralytics; CUDA build installed automatically on Colab
onnx>=1.14                # ONNX graph export
onnxruntime>=1.17         # ONNX CPU inference (validation cell)

# Data — TACO download & Open Images
requests>=2.28
fiftyone>=0.23            # Open Images v7 streaming (no API key required)
opencv-python>=4.8        # image I/O and preprocessing

# Numerics & visualisation
numpy>=1.24
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3

# Config serialisation
pyyaml>=6.0
